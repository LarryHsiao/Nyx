<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:dist="http://schemas.android.com/apk/distribution"
          package="com.larryhsiao.nyx">

    <application
            android:name=".NyxApplication"
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <intent-filter>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <action android:name="android.intent.action.VIEW"/>
                <data android:scheme="nyx" android:host="larryhsiao.com"/>
            </intent-filter>
        </activity>
        <activity
                android:name="com.larryhsiao.nyx.view.diary.NewDiaryActivity"/>
        <activity
                android:name="com.larryhsiao.nyx.view.diary.NewDiaryWidgetActivity"
                android:taskAffinity=""
                android:excludeFromRecents="true"
                android:clearTaskOnLaunch="true"
        />

        <receiver
                android:name="com.larryhsiao.nyx.view.diary.widget.NewDiaryWidgetProvider">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE"/>
            </intent-filter>
            <meta-data
                    android:name="android.appwidget.provider"
                    android:resource="@xml/widget_new_diary"/>
        </receiver>

        <meta-data
                android:name="firebase_crashlytics_collection_enabled"
                android:value="true"/>

        <meta-data
                android:name="firebase_performance_logcat_enabled"
                android:value="true"/>
    </application>

    <dist:module dist:instant="true"/>
</manifest>